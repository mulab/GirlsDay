version: '2'
services:
  web:
    build: .
    restart: always
    networks:
      - globalproxy_default
    volumes:
      - database:db.sqlite3
    restart: always
volumes:
  web:
    driver: local
networks:
  globalproxy_default:
    external: true
